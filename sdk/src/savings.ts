export function deposit(amount: bigint) {
  // placeholder
  return amount;
}

export function withdraw(amount: bigint) {
  // placeholder
  return amount;
}

export function sharePrice(): number {
  return 1;
}
